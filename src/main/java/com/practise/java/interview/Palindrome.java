package com.practise.java.interview;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Stack;
import java.util.stream.IntStream;

public class Palindrome {
    public static void main(String[] args) {
        String a = "able";
        String b = "elba";
//        Stack<String> aStack = new Stack<String>(a.split(""));
//        Arrays.asList(a.split("")).stream().map(i -> Character.valueOf(i)) ->

        //Given a positive integer, Find Maximum number possible by doing at-most K swaps where each number can be swapped with adjacent number only.
        //1234 -> 2134 (One swap)
        //1234->2314(Two Swaps)
//        List<Integer> integerList = new ArrayList<>();
//        IntStream.range(0, k).forEach(
//                integerList.stream().limit(integerList.size()-1).peek(i -> {
//                    if(integerList.get(i) < integerList.get(i+1)) {
////                //swap
////            }
//                }})
//        );
    }
}
