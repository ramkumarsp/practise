package com.learning.check;

import java.util.concurrent.atomic.AtomicInteger;
import java.util.stream.IntStream;

public class FactorialOfaNumberInJava8 {
    public static void main(String[] args) {
//        int n = 5;
//        AtomicInteger factorial = new AtomicInteger(1);
//        IntStream.rangeClosed(2, n).forEach(i -> factorial.getAndSet(factorial.get()) * i);
//        System.out.println(factorial.get());
    }
}
